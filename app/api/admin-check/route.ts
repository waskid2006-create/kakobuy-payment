import { cookies } from "next/headers" export async function GET() { try { const cookieStore = await cookies() const adminCookie = cookieStore.get("kakobuy_admin") if ( !adminCookie || adminCookie.value !== "authenticated" ) { return Response.json( { authenticated: false, }, { status: 401 } ) } return Response.json({ authenticated: true, }) } catch (error) { console.error( "Admin check error:", error ) return Response.json( { authenticated: false, }, { status: 401 } ) } } 
